#------------------------------------------
# Param�tres standard SpringBoot
#------------------------------------------
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.http.multipart.enabled=false

spring.datasource.url=jdbc:mysql://${database.host}/insee_data
spring.datasource.username=${database.login}
spring.datasource.password=${database.password}
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

logging.level.root=INFO
logging.level.org.springframework.web=INFO

#---------------------------------------------
# Param�tres de configuration de l'application
#---------------------------------------------

#Le chemin dans lequel seront mis les fichiers charg�s
formulaire.app.chemin.chargement.fichiers=/chargement

#Le login/mot de passe pour acc�der � l'application
formulaire.app.utilisateur.login=${user.login}
formulaire.app.utilisateur.mot_de_passe=${user.password}

#Clef prot�geant l'API
formulaire.app.clef.api=${api.key}
